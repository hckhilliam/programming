from collections import defaultdict, deque
import functools
import math
import re
from utils import p_rows, to_array

debug = False


def part1(data):
    rows = parse_data(data)


def part2(data):
    rows = parse_data(data)


def parse_data(data):
    rows = data.split('\n')
    return rows
